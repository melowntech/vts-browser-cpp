#include <unordered_map>

#include <renderer/fetcher.h>

#include "cache.h"

namespace melown
{
    Cache::Cache(const MapOptions &options)
    {

    }

    Cache::~Cache()
    {
    }

    void Cache::purge(const std::string &name)
    {

    }

    void Cache::purgeAll()
    {

    }

    bool Cache::read(const std::string &name, void *&buffer, uint32 &size)
    {
        return false;
    }
}
